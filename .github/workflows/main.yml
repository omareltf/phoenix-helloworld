name: credo
on: [push]
jobs:
  credo:
    name: runner / credo
    runs-on: ubuntu-latest
    steps:
        - uses: actions/checkout@v4
        - name: Run Credo
          uses: omareltf/my-credo@latest
          with:
            args: --config-file /credo.exs
