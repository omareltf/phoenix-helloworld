name: credo
on: [push]
jobs:
  credo:
    name: runner / credo
    runs-on: ubuntu-latest
    steps:
        - uses: actions/checkout@v3
        - name: Run Credo
          uses: omareltf/my-credo@v1
          with:
            args: --only warning
        - run: pwd
        - run: ls /github/workspace/
